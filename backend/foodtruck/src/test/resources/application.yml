spring:
# jpa
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: validate
# h2 in-memory db
  datasource:
    url: jdbc:h2:mem:test;MODE=MySQL
# disable test db replace
  test:
    database:
      replace: NONE
